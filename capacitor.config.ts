import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appReportes-Clientes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
